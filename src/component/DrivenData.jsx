const DrivenData = (props) => {
    return (
        <div>
            <h1>this is driven for app.jsx</h1>
            <h2>{props.title}</h2>
            <h3>{props.mess}</h3>
        </div>
    );
};

export default DrivenData;